{ ... }:

{
  stylix.targets.kitty = {
    enable = true;

    colors.enable = true;
    fonts.enable = true;
    inputs.enable = true;
    opacity.enable = true;

    # Optional: richer palette mode
    variant256Colors = false;
  };
}
